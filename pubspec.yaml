name: spotify_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  dio: ^5.8.0+1
  get: ^4.7.2
  flutter_dotenv: ^5.2.1
  get_storage: ^2.1.1
  logger: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
  assets:
    - .env
    - test/.env.test